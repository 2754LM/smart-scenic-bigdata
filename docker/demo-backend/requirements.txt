fastapi==0.110.0
uvicorn[standard]==0.27.1
pymysql==1.1.0
pydantic==2.6.1
pandas==2.1.4
numpy==1.26.2
scikit-learn==1.3.2
# pyhive + thrift: connect to HiveServer2 over Thrift (port 10000)
thrift==0.20.0
thrift-sasl==0.4.3
PyHive==0.7.0
# Note: pyspark removed (heavy ~600MB, needs Java).
# Backend uses sklearn fallback (models trained by spark-master can still be loaded via docker exec).
# To enable dual-track PySpark mode, install pyspark + JDK manually and set USE_PYSPARK_MODELS=true.
